NODE_ENV=development
PORT=3000

# SQLite keeps setup to zero commands. Swapping to MySQL is a dialect change in
# src/config/database.js — the models and migrations don't care either way.
DB_STORAGE=./data/splitwise.sqlite
DB_LOGGING=false

# Used when an expense doesn't specify a currency and the payer has no default.
DEFAULT_CURRENCY=INR
